import { Content, SectionTitle } from "../../GlobalStyle";

// Functional Component
export const About = () => {
	// Rendering
	return (
		<Content>
			<SectionTitle>About</SectionTitle>
		</Content>
	);
};
